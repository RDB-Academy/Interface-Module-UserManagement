import Ember from 'ember';

/**
  Use to group one (or more) ProgressBar components inside it.

  @class Progress
  @namespace Components
  @extends Ember.Component
  @public
  */
export default Ember.Component.extend({
  classNames: ['progress']
});
