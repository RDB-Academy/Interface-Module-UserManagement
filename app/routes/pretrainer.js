import Ember from 'ember';

export default Ember.Route.extend({

  actions: {
        routeaction:  function(Check1, stuff2) {
               console.log(Check1);
               console.log(stuff2);

        }
    }

});
